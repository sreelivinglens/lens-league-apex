[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300 --keep-alive 5 --graceful-timeout 300"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
